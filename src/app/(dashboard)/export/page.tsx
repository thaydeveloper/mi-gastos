/**
 * @fileoverview Export page.
 */

import { ExportPageContainer } from '@/components/export/containers/ExportPageContainer';

export const metadata = {
  title: 'Exportar',
};

export default function ExportPage() {
  return <ExportPageContainer />;
}
